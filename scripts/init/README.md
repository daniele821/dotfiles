## script to have on usb drive, to easily initialize the distro

- copy `basic_installer.sh` into a new file called `installer.sh`
- on every branch, it it necessary to modify everywhere `TODO` is written

- `installer.sh` file can then be saved on usb to fastly restore backup


